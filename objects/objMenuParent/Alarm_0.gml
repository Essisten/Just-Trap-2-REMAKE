/// @description There should go menu options
optionsNum = array_length(strSelect);
if (array_length(valueType) == 1)
{
    for (var i = 0; i < optionsNum; i++)
    {
        valueType[i] = -1;
    }
}

