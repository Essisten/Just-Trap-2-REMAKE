/// @description There should go menu options
optionsNum = array_length_1d(strSelect);
if (array_length_1d(valueType) == 1)
{
    for (i = 0; i < optionsNum; i++)
    {
        valueType[i] = -1;
    }
}

