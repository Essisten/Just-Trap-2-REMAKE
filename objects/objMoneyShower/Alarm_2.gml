/// @description Cool way of showing a number
fake_reward += ceil(reward / 10);
reward_length = string_length(string(fake_reward));
if (fake_reward >= reward)
{
    fake_reward = reward;
   exit;
}
alarm[2] = 5;

