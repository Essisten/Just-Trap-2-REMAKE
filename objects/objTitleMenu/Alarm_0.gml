/// @description Sliding in

if (titleX < 400)
    titleX = min(titleX + spd, 400);

if (titleX < 400)
    alarm[0] = 1;
else
{
    //with (instance_create(0, 0, objLightEffect))
    //    spd = 0.03;
}

